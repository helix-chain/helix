.venv/
__pycache__/
*.pyc
.env
*.key
node_modules/

# Test artifacts
fp_test_report_*.json
replay_test_report_*.json
mainnet_test_log.json
*_backup.py
patch_*.py
clean_contracts_*.py
fp_test_runner.py

# Internal docs & business materials — NEVER push to public repo
docs/
CLAUDE.md
CODE_MAP.md
AGENTS.md
.claude/

# Local marketing / screenshot assets (README does not reference them)
*.png

# Local backups & versioned local artifacts (keep local, never delete)
*.bak_before_*
*_backup_*.py
p0a_verify_*.py

# Local test reports (append-only local history)
ci_smoke_report_*.json
proxy_fp_test_report_*.json
node/target/
hpl/target/

# HPL spike — local runtime ledger (never commit)
hpl_ledger.json

# Gated immune-library data — the MOAT. NEVER push to public repo.
# The public immune_library_500.json is the open SEED and stays public;
# these patterns fence the continuously-growing, gated assets.
# (See the disclosure-boundary policy under docs/.)
_gated/
immune_*live*.json
immune_library_*_gated*.json
*_telemetry_*.json
signature_pipeline/
scoring_weights*.json
attack_replays_gated/
